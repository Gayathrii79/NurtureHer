apiVersion: 1

providers:
  - name: NurtureHer
    orgId: 1
    folder: NurtureHer
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

