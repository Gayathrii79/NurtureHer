{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "type": "stat",
      "title": "HTTP Requests",
      "targets": [{"expr": "sum(rate(nurtureher_http_requests_total[5m]))"}],
      "gridPos": {"x": 0, "y": 0, "w": 8, "h": 4}
    },
    {
      "type": "timeseries",
      "title": "Request Latency p95",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(nurtureher_http_request_duration_seconds_bucket[5m])) by (le))"}],
      "gridPos": {"x": 8, "y": 0, "w": 8, "h": 4}
    },
    {
      "type": "timeseries",
      "title": "5xx Errors",
      "targets": [{"expr": "sum(rate(nurtureher_http_requests_total{status_code=~\"5..\"}[5m]))"}],
      "gridPos": {"x": 16, "y": 0, "w": 8, "h": 4}
    }
  ],
  "schemaVersion": 39,
  "tags": ["nurtureher"],
  "title": "NurtureHer API",
  "version": 1
}

